diff --git a/chrome/browser/ui/extensions/extensions_menu_view_model.cc b/chrome/browser/ui/extensions/extensions_menu_view_model.cc
index ab8c4058e85a8b79073892d42ee7d53eb40adecb..8d4adb44fbbafa793e194b928ac793fb1eb8547b 100644
--- a/chrome/browser/ui/extensions/extensions_menu_view_model.cc
+++ b/chrome/browser/ui/extensions/extensions_menu_view_model.cc
@@ -572,23 +572,31 @@ void ExtensionsMenuViewModel::UpdateSiteAccess(
     const extensions::ExtensionId& extension_id,
     const url::Origin& target_origin,
     PermissionsManager::UserSiteAccess site_access) {
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
+
   LogSiteAccessUpdate(site_access);
 
   Profile* profile = browser_->GetProfile();
   const extensions::Extension* extension = GetExtension(*profile, extension_id);
-  auto url = GetActiveWebContents()->GetLastCommittedURL();
+  auto url = web_contents->GetLastCommittedURL();
   if (extension->permissions_data()->IsRestrictedUrl(url, nullptr)) {
     return;
   }
 
   SitePermissionsHelper permissions(profile);
-  permissions.UpdateSiteAccess(*extension, GetActiveWebContents(), site_access,
+  permissions.UpdateSiteAccess(*extension, web_contents, site_access,
                                target_origin);
 }
 
 void ExtensionsMenuViewModel::AllowHostAccessRequest(
     const extensions::ExtensionId& extension_id) {
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
   extensions::ExtensionActionRunner* action_runner =
       extensions::ExtensionActionRunner::GetForWebContents(web_contents);
   if (!action_runner) {
@@ -611,6 +619,9 @@ void ExtensionsMenuViewModel::DismissHostAccessRequest(
   auto* permissions_manager = PermissionsManager::Get(browser_->GetProfile());
   CHECK(permissions_manager);
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
   int tab_id = extensions::ExtensionTabUtil::GetTabId(web_contents);
   permissions_manager->UserDismissedHostAccessRequest(web_contents, tab_id,
                                                       extension_id);
@@ -634,6 +645,9 @@ void ExtensionsMenuViewModel::GrantSiteAccess(
   auto* profile = browser_->GetProfile();
   const extensions::Extension* extension = GetExtension(*profile, extension_id);
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
 
   // Verify that the origin displayed when the action was initiated matches the
   // current origin of the WebContents.
@@ -697,6 +711,9 @@ void ExtensionsMenuViewModel::RevokeSiteAccess(
   auto* profile = browser_->GetProfile();
   const extensions::Extension* extension = GetExtension(*profile, extension_id);
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
 
   // Verify that the origin displayed when the action was initiated matches the
   // current origin of the WebContents.
@@ -749,6 +766,10 @@ void ExtensionsMenuViewModel::RevokeSiteAccess(
 
 void ExtensionsMenuViewModel::ExecuteAction(
     const extensions::ExtensionId& extension_id) {
+  if (!GetActiveWebContents()) {
+    return;
+  }
+
   ExtensionActionViewModel* action_model = GetActionViewModel(extension_id);
   if (!action_model) {
     return;
@@ -763,8 +784,11 @@ void ExtensionsMenuViewModel::ExecuteAction(
 void ExtensionsMenuViewModel::UpdateSiteSetting(
     extensions::PermissionsManager::UserSiteSetting site_setting) {
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
   const url::Origin& origin =
-      GetActiveWebContents()->GetPrimaryMainFrame()->GetLastCommittedOrigin();
+      web_contents->GetPrimaryMainFrame()->GetLastCommittedOrigin();
 
   if (origin.opaque()) {
     return;
@@ -779,13 +803,19 @@ void ExtensionsMenuViewModel::UpdateSiteSetting(
 }
 
 void ExtensionsMenuViewModel::ReloadWebContents() {
-  GetActiveWebContents()->GetController().Reload(content::ReloadType::NORMAL,
-                                                 false);
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
+  web_contents->GetController().Reload(content::ReloadType::NORMAL, false);
 }
 
 bool ExtensionsMenuViewModel::CanShowSitePermissionsPage(
     const extensions::ExtensionId& extension_id) {
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return false;
+  }
   Profile* profile = browser_->GetProfile();
   ExtensionActionViewModel* action_model = GetActionViewModel(extension_id);
   CHECK(action_model);
@@ -815,7 +845,7 @@ ExtensionsMenuViewModel::GetActionButtonState(
   button_state.accessible_name = button_state.text;
   button_state.tooltip_text = action_model->GetTooltip(web_contents);
   button_state.status =
-      action_model->IsEnabled(web_contents)
+      web_contents && action_model->IsEnabled(web_contents)
           ? ExtensionsMenuViewModel::ControlState::Status::kEnabled
           : ExtensionsMenuViewModel::ControlState::Status::kDisabled;
   button_state.icon = action_model->GetIcon(web_contents, icon_size);
@@ -874,13 +904,17 @@ ExtensionsMenuViewModel::ExtensionSitePermissionsState
 ExtensionsMenuViewModel::GetExtensionSitePermissionsState(
     const extensions::ExtensionId& extension_id,
     const gfx::Size& icon_size) {
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return ExtensionSitePermissionsState();
+  }
+
   Profile* profile = browser_->GetProfile();
   auto* permissions_manager = PermissionsManager::Get(profile);
 
   ExtensionActionViewModel* action_model = GetActionViewModel(extension_id);
   const extensions::Extension* extension = action_model->GetExtension();
   CHECK(extension);
-  content::WebContents* web_contents = GetActiveWebContents();
   const GURL& url = web_contents->GetLastCommittedURL();
 
   // Extension's site permissions can only be computed when site access can be
@@ -962,13 +996,17 @@ ExtensionsMenuViewModel::GetMenuEntryState(
   entry_state.action_button =
       GetActionButtonState(extension_id, action_icon_size);
   entry_state.context_menu_button = GetContextMenuButtonState(action_model);
+  entry_state.is_enterprise = extensions::ExtensionSystem::Get(profile)
+                                  ->management_policy()
+                                  ->HasEnterpriseForcedAccess(*extension);
+  if (!web_contents) {
+    return entry_state;
+  }
+
   entry_state.site_access_toggle = GetSiteAccessToggleState(
       *extension, *profile, *toolbar_model_, *web_contents);
   entry_state.site_permissions_button = GetSitePermissionsButtonState(
       *extension, *profile, *toolbar_model_, *web_contents);
-  entry_state.is_enterprise = extensions::ExtensionSystem::Get(profile)
-                                  ->management_policy()
-                                  ->HasEnterpriseForcedAccess(*extension);
   entry_state.origin =
       web_contents->GetPrimaryMainFrame()->GetLastCommittedOrigin();
 
@@ -991,6 +1029,9 @@ ExtensionsMenuViewModel::GetMenuEntryState(
 ExtensionsMenuViewModel::OptionalSection
 ExtensionsMenuViewModel::GetOptionalSection() {
   content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return ExtensionsMenuViewModel::OptionalSection::kNone;
+  }
   auto reload_required = [web_contents]() {
     return extensions::TabHelper::FromWebContents(web_contents)
         ->IsReloadRequired();
@@ -1087,6 +1128,9 @@ void ExtensionsMenuViewModel::OnHostAccessRequestAdded(
     int tab_id) {
   // Ignore requests for other tabs.
   auto* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
   int current_tab_id = extensions::ExtensionTabUtil::GetTabId(web_contents);
   if (tab_id != current_tab_id) {
     return;
@@ -1112,8 +1156,11 @@ void ExtensionsMenuViewModel::OnHostAccessRequestUpdated(
     const extensions::ExtensionId& extension_id,
     int tab_id) {
   // Ignore requests for other tabs.
-  int current_tab_id =
-      extensions::ExtensionTabUtil::GetTabId(GetActiveWebContents());
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
+  int current_tab_id = extensions::ExtensionTabUtil::GetTabId(web_contents);
   if (tab_id != current_tab_id) {
     return;
   }
@@ -1153,6 +1200,9 @@ void ExtensionsMenuViewModel::OnHostAccessRequestRemoved(
     int tab_id) {
   // Ignore requests for other tabs.
   auto* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
   int current_tab_id = extensions::ExtensionTabUtil::GetTabId(web_contents);
   if (tab_id != current_tab_id) {
     return;
@@ -1163,8 +1213,12 @@ void ExtensionsMenuViewModel::OnHostAccessRequestRemoved(
 
 void ExtensionsMenuViewModel::OnHostAccessRequestsCleared(int tab_id) {
   // Ignore requests for other tabs.
-  int current_tab_id =
-      extensions::ExtensionTabUtil::GetTabId(GetActiveWebContents());
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    host_access_requests_.clear();
+    return;
+  }
+  int current_tab_id = extensions::ExtensionTabUtil::GetTabId(web_contents);
   if (tab_id != current_tab_id) {
     return;
   }
@@ -1182,8 +1236,9 @@ void ExtensionsMenuViewModel::OnHostAccessRequestDismissedByUser(
   // Ignore request dismissal if web contents have navigated to a different
   // origin from where the request originated, as navigation listeners will
   // handle menu updates.
-  if (GetActiveWebContents()->GetPrimaryMainFrame()->GetLastCommittedOrigin() !=
-      origin) {
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents ||
+      web_contents->GetPrimaryMainFrame()->GetLastCommittedOrigin() != origin) {
     return;
   }
 
@@ -1301,7 +1356,7 @@ void ExtensionsMenuViewModel::OnActiveTabChanged(TabListInterface& tab_list,
   if (!tab_list_interface_observation_.IsObserving()) {
     return;
   }
-  auto* web_contents = tab->GetContents();
+  auto* web_contents = tab ? tab->GetContents() : nullptr;
   WebContentsObserver::Observe(web_contents);
 
   OnWebContentsChanged(web_contents);
@@ -1401,12 +1456,17 @@ void ExtensionsMenuViewModel::RemoveHostAccessRequest(
 void ExtensionsMenuViewModel::UpdateHostAccessRequests() {
   host_access_requests_.clear();
 
+  content::WebContents* web_contents = GetActiveWebContents();
+  if (!web_contents) {
+    return;
+  }
+
   // Store the extension ids for the actions that have an active request.
   // Since action_models_ is sorted, iterating through it ensures
   // host_access_requests_ is also sorted.
   auto* permissions_manager =
       extensions::PermissionsManager::Get(browser_->GetProfile());
-  int tab_id = extensions::ExtensionTabUtil::GetTabId(GetActiveWebContents());
+  int tab_id = extensions::ExtensionTabUtil::GetTabId(web_contents);
 
   for (const auto& action_model : action_models()) {
     auto extension_id = action_model->GetId();
diff --git a/chrome/browser/ui/extensions/extensions_menu_view_model.h b/chrome/browser/ui/extensions/extensions_menu_view_model.h
index 65e40b94a90768b2c0db8a1fb0e8c029fe7b0156..270f26ad772faafbbfa93334789bc534ea92cd85 100644
--- a/chrome/browser/ui/extensions/extensions_menu_view_model.h
+++ b/chrome/browser/ui/extensions/extensions_menu_view_model.h
@@ -303,9 +303,9 @@ class ExtensionsMenuViewModel : public extensions::PermissionsManager::Observer,
       const extensions::ExtensionId& extension_id,
       const gfx::Size& icon_size);
 
-  // Returns the site access permissions state for an extension. This will crash
-  // if called when the user cannot modify the extension site permissions, as
-  // this method would compute invalid values.
+  // Returns the site access permissions state for an extension. Returns a
+  // default state with hidden controls when no active WebContents exists. All
+  // other callers must first verify CanShowSitePermissionsPage().
   ExtensionSitePermissionsState GetExtensionSitePermissionsState(
       const extensions::ExtensionId& extension_id,
       const gfx::Size& icon_size);
diff --git a/chrome/browser/ui/extensions/extensions_menu_view_model_browsertest.cc b/chrome/browser/ui/extensions/extensions_menu_view_model_browsertest.cc
index 22a4cf07a06b51f4d71243eb6bf557db4b2bd39a..8eee890bc3ec67567dfabfb54e68894f120dbedb 100644
--- a/chrome/browser/ui/extensions/extensions_menu_view_model_browsertest.cc
+++ b/chrome/browser/ui/extensions/extensions_menu_view_model_browsertest.cc
@@ -8,11 +8,13 @@
 
 #include "base/memory/scoped_refptr.h"
 #include "base/test/metrics/user_action_tester.h"
+#include "build/build_config.h"
 #include "chrome/browser/extensions/extension_action_runner.h"
 #include "chrome/browser/extensions/extension_browsertest.h"
 #include "chrome/browser/extensions/extension_tab_util.h"
 #include "chrome/browser/extensions/extension_view_host.h"
 #include "chrome/browser/profiles/profile.h"
+#include "chrome/browser/ui/browser_tabstrip.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
 #include "chrome/browser/ui/extensions/extension_action_delegate.h"
 #include "chrome/grit/generated_resources.h"
@@ -1403,6 +1405,107 @@ IN_PROC_BROWSER_TEST_F(ExtensionsMenuViewModelBrowserTest,
   EXPECT_EQ(actions[2]->GetActionName(), u"Gamma");
 }
 
+#if !BUILDFLAG(IS_ANDROID)
+// Ahoi deliberately supports a live browser window without an active tab.
+// The extensions menu remains useful for managing installed extensions in
+// that state, but every site-bound control must be hidden or disabled.
+IN_PROC_BROWSER_TEST_F(ExtensionsMenuViewModelBrowserTest,
+                       EmptyTabListReturnsGenericExtensionState) {
+  auto extension =
+      AddExtensionWithHostPermission("Extension", "<all_urls>");
+  const GURL url =
+      embedded_test_server()->GetURL("example.com", "/simple.html");
+  ASSERT_TRUE(NavigateToURL(GetActiveWebContents(), url));
+  const url::Origin origin = url::Origin::Create(url);
+  const auto site_access_before =
+      permissions_manager()->GetUserSiteAccess(*extension, url);
+  const auto site_setting_before =
+      permissions_manager()->GetUserSiteSetting(origin);
+  base::UserActionTester user_action_tester;
+  constexpr char kActivatedUserAction[] =
+      "Extensions.Toolbar.ExtensionActivatedFromMenu";
+
+  ASSERT_EQ(1, browser()->tab_strip_model()->count());
+
+  browser()->tab_strip_model()->DetachAndDeleteWebContentsAt(0);
+
+  ASSERT_EQ(0, browser()->tab_strip_model()->count());
+  ASSERT_EQ(nullptr, menu_model()->GetActiveWebContents());
+  EXPECT_EQ(menu_model()->GetOptionalSection(),
+            ExtensionsMenuViewModel::OptionalSection::kNone);
+  EXPECT_EQ(menu_model()->GetSiteSettingsState().toggle.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+
+  ExtensionsMenuViewModel::MenuEntryState state =
+      menu_model()->GetMenuEntryState(extension->id(), gfx::Size(16, 16));
+  EXPECT_EQ(state.action_button.status,
+            ExtensionsMenuViewModel::ControlState::Status::kDisabled);
+  EXPECT_FALSE(state.action_button.icon.IsEmpty());
+  EXPECT_EQ(state.site_access_toggle.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+  EXPECT_EQ(state.site_permissions_button.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+  EXPECT_FALSE(menu_model()->CanShowSitePermissionsPage(extension->id()));
+
+  ExtensionsMenuViewModel::ExtensionSitePermissionsState permissions_state =
+      menu_model()->GetExtensionSitePermissionsState(extension->id(),
+                                                     gfx::Size(16, 16));
+  EXPECT_EQ(permissions_state.on_click_option.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+  EXPECT_EQ(permissions_state.on_site_option.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+  EXPECT_EQ(permissions_state.on_all_sites_option.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+  EXPECT_EQ(permissions_state.show_requests_toggle.status,
+            ExtensionsMenuViewModel::ControlState::Status::kHidden);
+
+  // Every callback that was bound to the former page must fail closed after
+  // its tab disappeared. In particular, it must neither change permissions
+  // nor record a successful extension action.
+  menu_model()->UpdateSiteAccess(
+      extension->id(), origin, PermissionsManager::UserSiteAccess::kOnClick);
+  menu_model()->GrantSiteAccess(extension->id(), origin);
+  menu_model()->RevokeSiteAccess(extension->id(), origin);
+  menu_model()->AllowHostAccessRequest(extension->id());
+  menu_model()->DismissHostAccessRequest(extension->id());
+  menu_model()->ExecuteAction(extension->id());
+  menu_model()->UpdateSiteSetting(
+      PermissionsManager::UserSiteSetting::kBlockAllExtensions);
+  menu_model()->ReloadWebContents();
+  menu_model()->OnHostAccessRequestAdded(extension->id(), 0);
+  menu_model()->OnHostAccessRequestUpdated(extension->id(), 0);
+  menu_model()->OnHostAccessRequestRemoved(extension->id(), 0);
+  menu_model()->OnHostAccessRequestsCleared(0);
+  menu_model()->OnHostAccessRequestDismissedByUser(extension->id(), origin);
+
+  EXPECT_EQ(permissions_manager()->GetUserSiteAccess(*extension, url),
+            site_access_before);
+  EXPECT_EQ(permissions_manager()->GetUserSiteSetting(origin),
+            site_setting_before);
+  EXPECT_EQ(user_action_tester.GetActionCount(kActivatedUserAction), 0);
+
+  content::WebContents* restored_contents = chrome::AddAndReturnTabAt(
+      browser_window_interface(), GURL(), -1, true);
+  ASSERT_TRUE(restored_contents);
+  ASSERT_TRUE(NavigateToURL(restored_contents, url));
+  ASSERT_EQ(restored_contents, menu_model()->GetActiveWebContents());
+  EXPECT_TRUE(menu_model()->CanShowSitePermissionsPage(extension->id()));
+
+  state = menu_model()->GetMenuEntryState(extension->id(), gfx::Size(16, 16));
+  EXPECT_EQ(state.action_button.status,
+            ExtensionsMenuViewModel::ControlState::Status::kEnabled);
+  EXPECT_EQ(state.site_access_toggle.status,
+            ExtensionsMenuViewModel::ControlState::Status::kEnabled);
+  EXPECT_EQ(state.site_permissions_button.status,
+            ExtensionsMenuViewModel::ControlState::Status::kEnabled);
+
+  browser()->tab_strip_model()->DetachAndDeleteWebContentsAt(0);
+  EXPECT_EQ(0, browser()->tab_strip_model()->count());
+  EXPECT_EQ(nullptr, menu_model()->GetActiveWebContents());
+  EXPECT_FALSE(menu_model()->CanShowSitePermissionsPage(extension->id()));
+}
+#endif  // !BUILDFLAG(IS_ANDROID)
+
 // Tests that host access requests are maintained in alphabetical order
 // matching the action_models_ order, regardless of the order they are added.
 IN_PROC_BROWSER_TEST_F(ExtensionsMenuViewModelBrowserTest,
diff --git a/chrome/browser/ui/views/extensions/extensions_menu_delegate_desktop.cc b/chrome/browser/ui/views/extensions/extensions_menu_delegate_desktop.cc
index fc2080daf61b701ec286dad419ee43db08b1d6d2..a7b9b497f4257d6f455d718283d86d2c595a809f 100644
--- a/chrome/browser/ui/views/extensions/extensions_menu_delegate_desktop.cc
+++ b/chrome/browser/ui/views/extensions/extensions_menu_delegate_desktop.cc
@@ -381,12 +381,13 @@ void ExtensionsMenuDelegateDesktop::OnUserPermissionsSettingsChanged() {
     // Site permissions page can only be opened when site setting is set to
     // "customize by extension". Thus, when site settings changed, we have to
     // return to main page.
-    DCHECK_NE(PermissionsManager::Get(browser_->GetProfile())
-                  ->GetUserSiteSetting(browser_->GetTabStripModel()
-                                           ->GetActiveWebContents()
-                                           ->GetPrimaryMainFrame()
-                                           ->GetLastCommittedOrigin()),
-              PermissionsManager::UserSiteSetting::kCustomizeByExtension);
+    content::WebContents* web_contents = menu_model_->GetActiveWebContents();
+    if (web_contents) {
+      DCHECK_NE(PermissionsManager::Get(browser_->GetProfile())
+                    ->GetUserSiteSetting(web_contents->GetPrimaryMainFrame()
+                                             ->GetLastCommittedOrigin()),
+                PermissionsManager::UserSiteSetting::kCustomizeByExtension);
+    }
     OpenMainPage();
     return;
   }
@@ -412,7 +413,9 @@ void ExtensionsMenuDelegateDesktop::OpenMainPage() {
 
 void ExtensionsMenuDelegateDesktop::OpenSitePermissionsPage(
     const extensions::ExtensionId& extension_id) {
-  CHECK(menu_model_->CanShowSitePermissionsPage(extension_id));
+  if (!menu_model_->CanShowSitePermissionsPage(extension_id)) {
+    return;
+  }
 
   auto site_permissions_page =
       std::make_unique<ExtensionsMenuSitePermissionsPageView>(
diff --git a/chrome/browser/ui/views/extensions/extensions_menu_site_permissions_page_view_interactive_uitest.cc b/chrome/browser/ui/views/extensions/extensions_menu_site_permissions_page_view_interactive_uitest.cc
index 3e914fce906bdaaca76c4d6f0cbf1990c772d454..8e4261f5543f5d64aca34e2f93d1ed1b6f3e2210 100644
--- a/chrome/browser/ui/views/extensions/extensions_menu_site_permissions_page_view_interactive_uitest.cc
+++ b/chrome/browser/ui/views/extensions/extensions_menu_site_permissions_page_view_interactive_uitest.cc
@@ -183,3 +183,35 @@ IN_PROC_BROWSER_TEST_F(ExtensionsMenuSitePermissionsPageViewInteractiveUITest,
   // Verify that the site permissions page is no longer opened.
   EXPECT_FALSE(IsSitePermissionsPageOpened(extension->id()));
 }
+
+// Tests that removing Ahoi's last tab while the site permissions page is open
+// returns to a generic main page and ignores callbacks from the stale page.
+IN_PROC_BROWSER_TEST_F(ExtensionsMenuSitePermissionsPageViewInteractiveUITest,
+                       RemoveLastTabWhileSitePermissionsPageOpened) {
+  ASSERT_TRUE(embedded_test_server()->Start());
+  auto extension =
+      LoadTestExtension("extensions/blocked_actions/content_scripts");
+
+  const GURL url = embedded_test_server()->GetURL("/simple.html");
+  ASSERT_TRUE(ui_test_utils::NavigateToURL(browser(), url));
+
+  ShowUi(extension->id());
+  ASSERT_TRUE(IsSitePermissionsPageOpened(extension->id()));
+  ASSERT_FALSE(IsMainPageOpened());
+
+  browser()->tab_strip_model()->DetachAndDeleteWebContentsAt(0);
+  ASSERT_EQ(0, browser()->tab_strip_model()->count());
+  EXPECT_FALSE(IsSitePermissionsPageOpened(extension->id()));
+  ASSERT_TRUE(IsMainPageOpened());
+
+  ExtensionsMenuDelegateDesktop* menu_delegate =
+      menu_coordinator()->GetDelegateForTesting();
+  ASSERT_TRUE(menu_delegate);
+  menu_delegate->OpenSitePermissionsPage(extension->id());
+  EXPECT_TRUE(IsMainPageOpened());
+  EXPECT_FALSE(IsSitePermissionsPageOpened(extension->id()));
+
+  menu_delegate->OnUserPermissionsSettingsChanged();
+  EXPECT_TRUE(IsMainPageOpened());
+  EXPECT_FALSE(IsSitePermissionsPageOpened(extension->id()));
+}
