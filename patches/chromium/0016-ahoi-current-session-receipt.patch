diff --git a/chrome/browser/sessions/session_service.cc b/chrome/browser/sessions/session_service.cc
index db000e3621fe3794f1888bcf039dc79b92510082..4df8a9f395d3adc09bd8f50e3749734ab00d8233 100644
--- a/chrome/browser/sessions/session_service.cc
+++ b/chrome/browser/sessions/session_service.cc
@@ -11,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "ahoi/browser/session/session_restore_integration.h"
 #include "base/command_line.h"
 #include "base/debug/alias.h"
 #include "base/debug/dump_without_crashing.h"
@@ -41,6 +42,7 @@
 #include "chrome/browser/ui/browser_tabstrip.h"
 #include "chrome/browser/ui/browser_window.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface_iterator.h"
+#include "chrome/browser/ui/browser_window/public/profile_browser_collection.h"
 #include "chrome/browser/ui/session_crashed_bubble.h"
 #include "chrome/browser/ui/startup/profile_launch_observer.h"
 #include "chrome/browser/ui/startup/startup_browser_creator.h"
@@ -57,8 +59,10 @@
 #include "components/sessions/core/command_storage_manager.h"
 #include "components/sessions/core/session_command.h"
 #include "components/sessions/core/session_constants.h"
+#include "components/sessions/core/session_service_commands.h"
 #include "components/sessions/core/session_types.h"
 #include "components/sessions/core/tab_restore_service.h"
+#include "components/tabs/public/tab_interface.h"
 #include "content/public/browser/navigation_details.h"
 #include "content/public/browser/navigation_entry.h"
 #include "content/public/browser/session_storage_namespace.h"
@@ -245,6 +249,19 @@ void SessionService::DeleteLastSession() {
   ++count_delete_last_session_for_testing_;
 }
 
+void SessionService::ResetFlushAndReadCurrentSessionForVerification(
+    sessions::GetLastSessionCallback callback) {
+  if (!is_saving_enabled()) {
+    std::move(callback).Run({}, SessionID::InvalidValue(), true);
+    return;
+  }
+
+  ScheduleResetCommands();
+  command_storage_manager()->SaveAndReadCurrentSessionCommands(base::BindOnce(
+      &SessionService::OnGotCurrentSessionCommandsForVerification,
+      weak_factory_.GetWeakPtr(), std::move(callback)));
+}
+
 void SessionService::SetSplitTab(SessionID window_id,
                                  SessionID tab_id,
                                  std::optional<split_tabs::SplitTabId> split) {
@@ -637,6 +654,17 @@ void SessionService::BuildCommandsForTab(
       sessions::SessionTabHelper::FromWebContents(tab);
   SessionID session_id(session_tab_helper->session_id());
 
+  tabs::TabInterface* tab_interface = tabs::TabInterface::GetFromContents(tab);
+  std::map<std::string, std::string> extra_data;
+  if (ahoi::session::PopulateTabSessionExtraData(
+          tab_interface->GetBrowserWindowInterface(), tab_interface,
+          &extra_data)) {
+    for (const auto& [key, data] : extra_data) {
+      command_storage_manager()->AppendRebuildCommand(
+          sessions::CreateAddTabExtraDataCommand(session_id, key, data));
+    }
+  }
+
   const blink::UserAgentOverride& ua_override = tab->GetUserAgentOverride();
 
   if (!ua_override.ua_string_override.empty()) {
@@ -661,6 +689,23 @@ void SessionService::BuildCommandsForTab(
   }
 }
 
+void SessionService::BuildCommandsForBrowser(
+    Browser* browser,
+    IdToRange* tab_to_available_range,
+    std::set<SessionID>* windows_to_track) {
+  SessionServiceBase::BuildCommandsForBrowser(browser, tab_to_available_range,
+                                              windows_to_track);
+
+  std::map<std::string, std::string> extra_data;
+  if (ahoi::session::PopulateWindowSessionExtraData(browser, &extra_data)) {
+    for (const auto& [key, data] : extra_data) {
+      command_storage_manager()->AppendRebuildCommand(
+          sessions::CreateAddWindowExtraDataCommand(browser->session_id(), key,
+                                                    data));
+    }
+  }
+}
+
 void SessionService::ScheduleResetCommands() {
   DCHECK(is_saving_enabled());
   command_storage_manager()->set_pending_reset(true);
@@ -670,6 +715,24 @@ void SessionService::ScheduleResetCommands() {
   last_selected_tab_in_window()->clear();
   set_rebuild_on_next_save(false);
   BuildCommandsFromBrowsers(tab_to_available_range(), windows_tracking());
+  SessionID active_window_id = SessionID::InvalidValue();
+  ProfileBrowserCollection* browser_collection =
+      ProfileBrowserCollection::GetForProfile(profile());
+  if (browser_collection) {
+    browser_collection->ForEach(
+        [this, &active_window_id](BrowserWindowInterface* browser) {
+          if (!windows_tracking()->contains(browser->GetSessionID())) {
+            return true;
+          }
+          active_window_id = browser->GetSessionID();
+          return false;
+        },
+        BrowserCollection::Order::kActivation);
+  }
+  if (active_window_id.is_valid()) {
+    command_storage_manager()->AppendRebuildCommand(
+        sessions::CreateSetActiveWindowCommand(active_window_id));
+  }
   if (!windows_tracking()->empty()) {
     // We're lazily created on startup and won't get an initial batch of
     // SetWindowType messages. Set these here to make sure our state is correct.
@@ -679,6 +742,27 @@ void SessionService::ScheduleResetCommands() {
   command_storage_manager()->StartSaveTimer();
 }
 
+void SessionService::OnGotCurrentSessionCommandsForVerification(
+    sessions::GetLastSessionCallback callback,
+    std::vector<std::unique_ptr<sessions::SessionCommand>> commands,
+    bool read_error) {
+  if (read_error) {
+    std::move(callback).Run({}, SessionID::InvalidValue(), true);
+    return;
+  }
+
+  std::vector<std::unique_ptr<sessions::SessionWindow>> valid_windows;
+  SessionID active_window_id = SessionID::InvalidValue();
+  std::string platform_session_id;
+  std::set<SessionID> discarded_window_ids;
+  sessions::RestoreSessionFromCommands(commands, &valid_windows,
+                                       &active_window_id, &platform_session_id,
+                                       &discarded_window_ids);
+  RemoveUnusedRestoreWindows(&valid_windows);
+  std::move(callback).Run(std::move(valid_windows), active_window_id,
+                          read_error);
+}
+
 void SessionService::CommitPendingCloses() {
   for (auto id : pending_tab_close_ids_) {
     ScheduleCommand(sessions::CreateTabClosedCommand(id));
diff --git a/chrome/browser/sessions/session_service.h b/chrome/browser/sessions/session_service.h
index 57773c395c0b1b530b2cf9663b509e17adeeee4b..7e2f2be4671b5d4de32cca583a678847ebb1a0b4 100644
--- a/chrome/browser/sessions/session_service.h
+++ b/chrome/browser/sessions/session_service.h
@@ -56,6 +56,7 @@ using StartupTabs = std::vector<StartupTab>;
 // make a pass in SessionService to remove app related code.
 class SessionService : public SessionServiceBase {
   friend class SessionServiceTestHelper;
+
  public:
   // Creates a SessionService for the specified profile.
   explicit SessionService(Profile* profile);
@@ -91,6 +92,11 @@ class SessionService : public SessionServiceBase {
   // Deletes the last session.
   void DeleteLastSession();
 
+  // Rebuilds and durably flushes the complete current session, reads it back,
+  // and returns the decoded receipt without initializing a platform session.
+  void ResetFlushAndReadCurrentSessionForVerification(
+      sessions::GetLastSessionCallback callback);
+
   // Sets a tab's split ID, if any. Note that a split can't be across
   // multiple windows.
   void SetSplitTab(SessionID window_id,
@@ -211,10 +217,19 @@ class SessionService : public SessionServiceBase {
                            bool is_pinned,
                            IdToRange* tab_to_available_range) override;
 
+  void BuildCommandsForBrowser(Browser* browser,
+                               IdToRange* tab_to_available_range,
+                               std::set<SessionID>* windows_to_track) override;
+
   // Schedules a reset of the existing commands. A reset means the contents
   // of the file are recreated from the state of the browser.
   void ScheduleResetCommands() override;
 
+  void OnGotCurrentSessionCommandsForVerification(
+      sessions::GetLastSessionCallback callback,
+      std::vector<std::unique_ptr<sessions::SessionCommand>> commands,
+      bool read_error);
+
   // Converts all pending tab/window closes to commands and schedules them.
   void CommitPendingCloses();
 
diff --git a/chrome/browser/sessions/session_service_browsertest.cc b/chrome/browser/sessions/session_service_browsertest.cc
index 31b98bb4f4d70787b99fce972c71d0e137baefa0..fcc305d59625aff99eee74087bb38f1b260fdbb4 100644
--- a/chrome/browser/sessions/session_service_browsertest.cc
+++ b/chrome/browser/sessions/session_service_browsertest.cc
@@ -7,6 +7,7 @@
 #include "base/containers/adapters.h"
 #include "base/run_loop.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/test/bind.h"
 #include "base/threading/thread_restrictions.h"
 #include "build/build_config.h"
 #include "chrome/browser/profiles/profile.h"
@@ -19,6 +20,7 @@
 #include "components/sessions/content/session_tab_helper.h"
 #include "components/sessions/core/command_storage_manager.h"
 #include "components/sessions/core/session_command.h"
+#include "components/sessions/core/session_types.h"
 #include "content/public/test/browser_test.h"
 
 #if BUILDFLAG(IS_CHROMEOS)
@@ -151,6 +153,33 @@ IN_PROC_BROWSER_TEST_F(SessionServiceBrowserTest, PinnedAfterReset) {
   EXPECT_TRUE(found_pinned_command);
 }
 
+IN_PROC_BROWSER_TEST_F(SessionServiceBrowserTest,
+                       ResetFlushAndReadCurrentSessionForVerification) {
+  std::vector<std::unique_ptr<sessions::SessionWindow>> windows;
+  SessionID active_window_id = SessionID::InvalidValue();
+  bool read_error = true;
+  base::RunLoop run_loop;
+
+  service()->ResetFlushAndReadCurrentSessionForVerification(
+      base::BindLambdaForTesting(
+          [&windows, &active_window_id, &read_error, &run_loop](
+              std::vector<std::unique_ptr<sessions::SessionWindow>> windows_out,
+              SessionID active_window_id_out, bool read_error_out) {
+            windows = std::move(windows_out);
+            active_window_id = active_window_id_out;
+            read_error = read_error_out;
+            run_loop.Quit();
+          }));
+  run_loop.Run();
+
+  EXPECT_FALSE(read_error);
+  ASSERT_EQ(1U, windows.size());
+  EXPECT_EQ(browser()->session_id(), windows[0]->window_id);
+  EXPECT_EQ(browser()->session_id(), active_window_id);
+  EXPECT_EQ(static_cast<size_t>(browser()->tab_strip_model()->count()),
+            windows[0]->tabs.size());
+}
+
 IN_PROC_BROWSER_TEST_F(SessionServiceBrowserTest, LogExit) {
   EXPECT_FALSE(FindMostRecentEventOfType(SessionServiceEventLogType::kExit));
   service()->WindowClosing(browser()->session_id());
diff --git a/components/sessions/core/command_storage_backend.cc b/components/sessions/core/command_storage_backend.cc
index ecab96800d28216be0fc6930e32cd870492965d0..383f444a099e7b03a8b94f64e02fb9f977ed6b0e 100644
--- a/components/sessions/core/command_storage_backend.cc
+++ b/components/sessions/core/command_storage_backend.cc
@@ -176,6 +176,7 @@ class SessionFileReader {
     // This is useful for reporting and debugging.
     std::vector<std::unique_ptr<sessions::SessionCommand>> commands;
     CommandStorageBackend::ReadStatus status = ReadStatus::kUnknown;
+    bool has_marker = false;
   };
 
   // Reads the state of commands from the specified file.
@@ -269,7 +270,9 @@ class SessionFileReader {
 SessionFileReader::ReadResult SessionFileReader::Read() {
   if (!IsHeaderValid()) {
     DCHECK_NE(read_status_, ReadStatus::kUnknown);
-    return {{}, read_status_};
+    ReadResult result;
+    result.status = read_status_;
+    return result;
   }
 
   ReadResult full_result;  // All commands that were successfully read.
@@ -277,6 +280,8 @@ SessionFileReader::ReadResult SessionFileReader::Read() {
   for (; !next_result.commands.empty(); next_result = ReadCommand()) {
     if (next_result.commands.front()->id() != kInitialStateMarkerCommandId) {
       full_result.commands.push_back(std::move(next_result.commands.front()));
+    } else {
+      full_result.has_marker = true;
     }
   }
 
@@ -615,6 +620,60 @@ CommandStorageBackend::ReadLastSessionCommands() {
   return result;
 }
 
+CommandStorageBackend::ReadCommandsResult
+CommandStorageBackend::FlushAndReadCurrentSessionCommands() {
+  InitIfNecessary();
+
+  CommandStorageBackend::ReadCommandsResult result;
+  if (!open_file_ || !open_file_->file || !open_file_->file->IsValid() ||
+      !open_file_->did_write_marker) {
+    result.error_reading = true;
+    return result;
+  }
+  if (is_encrypted() && (!encryptor_->IsEncryptionAvailable() ||
+                         !encryptor_->IsDecryptionAvailable())) {
+    result.error_reading = true;
+    return result;
+  }
+
+  const base::FilePath current_path = open_file_->path;
+  if (!open_file_->file->Flush()) {
+    result.error_reading = true;
+    return result;
+  }
+
+  // The writer intentionally denies parallel reads on Windows. Release that
+  // handle only after Flush(), then verify and decode the exact path before
+  // reopening it in append mode for subsequent session updates.
+  open_file_->file.reset();
+
+  SessionFileReader::ReadResult read_result =
+      SessionFileReader::Read(current_path, encryptor_.get());
+  if (!read_result.has_marker) {
+    read_result.commands.clear();
+    read_result.status = ReadStatus::kInvalidCommand;
+  }
+
+  std::unique_ptr<base::File> append_file = std::make_unique<base::File>(
+      current_path, base::File::FLAG_OPEN | base::File::FLAG_APPEND |
+                        base::File::FLAG_WIN_EXCLUSIVE_WRITE |
+                        base::File::FLAG_WIN_EXCLUSIVE_READ);
+  if (!append_file->IsValid() || !open_file_ ||
+      open_file_->path != current_path) {
+    CloseFile();
+    result.error_reading = true;
+    return result;
+  }
+  open_file_->file = std::move(append_file);
+
+  result.commands = std::move(read_result.commands);
+  result.error_reading = IsError(read_result.status);
+  if (result.error_reading) {
+    result.commands.clear();
+  }
+  return result;
+}
+
 void CommandStorageBackend::DeleteLastSession() {
   InitIfNecessary();
   if (last_session_info_) {
diff --git a/components/sessions/core/command_storage_backend.h b/components/sessions/core/command_storage_backend.h
index db51666c9731cf69e82508f737d90f4c7246029c..7f3efd5b5d4704c431c3ce7f7e58cbeb3636a06c 100644
--- a/components/sessions/core/command_storage_backend.h
+++ b/components/sessions/core/command_storage_backend.h
@@ -123,6 +123,12 @@ class SESSIONS_EXPORT CommandStorageBackend
   // Returns the commands from the last session file.
   ReadCommandsResult ReadLastSessionCommands();
 
+  // Flushes the current session file to durable storage, verifies that its
+  // initial-state marker is present, and decodes commands from that exact
+  // file. Returns an error when there is no complete current session file or
+  // when encryption/decryption is unavailable.
+  ReadCommandsResult FlushAndReadCurrentSessionCommands();
+
   // Deletes the file containing the commands for the last session.
   void DeleteLastSession();
 
diff --git a/components/sessions/core/command_storage_backend_unittest.cc b/components/sessions/core/command_storage_backend_unittest.cc
index a89d86ae526e9438fd65e90bec7ca6e5b9114aac..04c8ec54087968ae9150d240071aa50f79287147 100644
--- a/components/sessions/core/command_storage_backend_unittest.cc
+++ b/components/sessions/core/command_storage_backend_unittest.cc
@@ -645,6 +645,39 @@ TEST_P(CommandStorageBackendParamTest, SimpleReadWrite) {
       GetHistogramName("ReadLastSessionCommands", "", "Duration"), 1);
 }
 
+TEST_P(CommandStorageBackendParamTest, FlushAndReadCurrentSessionCommands) {
+  scoped_refptr<CommandStorageBackend> backend = CreateBackend();
+  auto data = std::to_array<TestData>({
+      {1, "current-a"},
+      {2, "current-b"},
+  });
+  SessionCommands commands;
+  commands.push_back(CreateCommandFromData(data[0]));
+  commands.push_back(CreateCommandFromData(data[1]));
+  backend->AppendCommands(std::move(commands), /*truncate=*/true,
+                          base::DoNothing());
+  const base::FilePath current_path = backend->current_path_for_testing();
+
+  ReadCommandsResult result = backend->FlushAndReadCurrentSessionCommands();
+
+  EXPECT_FALSE(result.error_reading);
+  ASSERT_EQ(data.size(), result.commands.size());
+  AssertCommandEqualsData(data[0], result.commands[0].get());
+  AssertCommandEqualsData(data[1], result.commands[1].get());
+  EXPECT_EQ(current_path, backend->current_path_for_testing());
+}
+
+TEST_P(CommandStorageBackendParamTest,
+       FlushAndReadCurrentSessionCommandsRequiresCompleteCurrentFile) {
+  scoped_refptr<CommandStorageBackend> backend = CreateBackend();
+
+  ReadCommandsResult result = backend->FlushAndReadCurrentSessionCommands();
+
+  EXPECT_TRUE(result.error_reading);
+  EXPECT_TRUE(result.commands.empty());
+  EXPECT_FALSE(backend->IsFileOpenForTesting());
+}
+
 TEST_P(CommandStorageBackendParamTest, RandomData) {
   auto data = std::to_array<TestData>({
       {1, "a"},
diff --git a/components/sessions/core/command_storage_manager.cc b/components/sessions/core/command_storage_manager.cc
index 30fc6e03a652537b00aaab12f9b98e59b6d6fa7d..c73486b3ed25248ecc50b97bf5505ed3e5e60c6f 100644
--- a/components/sessions/core/command_storage_manager.cc
+++ b/components/sessions/core/command_storage_manager.cc
@@ -55,6 +55,25 @@ bool CommandsEqual(const std::vector<std::unique_ptr<SessionCommand>>& a,
   });
 }
 
+CommandStorageBackend::ReadCommandsResult FlushAndReadDualBackends(
+    scoped_refptr<CommandStorageBackend> cleartext_backend,
+    scoped_refptr<CommandStorageBackend> encrypted_backend,
+    bool return_encrypted_commands) {
+  CommandStorageBackend::ReadCommandsResult cleartext_result =
+      cleartext_backend->FlushAndReadCurrentSessionCommands();
+  CommandStorageBackend::ReadCommandsResult encrypted_result =
+      encrypted_backend->FlushAndReadCurrentSessionCommands();
+
+  if (cleartext_result.error_reading || encrypted_result.error_reading ||
+      !CommandsEqual(cleartext_result.commands, encrypted_result.commands)) {
+    CommandStorageBackend::ReadCommandsResult error_result;
+    error_result.error_reading = true;
+    return error_result;
+  }
+  return return_encrypted_commands ? std::move(encrypted_result)
+                                   : std::move(cleartext_result);
+}
+
 // Result of comparing the commands read from the cleartext and encrypted
 // backends.
 // These values are persisted to logs. Entries should not be renumbered and
@@ -330,13 +349,17 @@ void CommandStorageManager::StartSaveTimer() {
 }
 
 void CommandStorageManager::Save() {
+  SaveImpl(/*allow_empty_reset=*/false);
+}
+
+void CommandStorageManager::SaveImpl(bool allow_empty_reset) {
   weak_factory_for_timer_.InvalidateWeakPtrs();
 
   // Inform the delegate that we will save the commands now, giving it the
   // opportunity to append more commands.
   delegate_->OnWillSaveCommands();
 
-  if (pending_commands_.empty()) {
+  if (pending_commands_.empty() && (!allow_empty_reset || !pending_reset_)) {
     return;
   }
 
@@ -427,6 +450,55 @@ void CommandStorageManager::Save() {
   }
 }
 
+void CommandStorageManager::SaveAndReadCurrentSessionCommands(
+    GetCommandsCallback callback) {
+  const EncryptSessionStorageStage encrypt_stage =
+      GetEncryptSessionStorageStage();
+  const bool requires_encrypted_backend =
+      encrypt_stage != EncryptSessionStorageStage::kClearOnly;
+  if (requires_encrypted_backend && !encrypted_backend_) {
+    weak_factory_for_timer_.InvalidateWeakPtrs();
+    LogEncryptedBackendUninitialized(
+        SessionEncryptedBackendUninitialized::kSave);
+    std::move(callback).Run({}, true);
+    return;
+  }
+
+  SaveImpl(/*allow_empty_reset=*/true);
+
+  switch (encrypt_stage) {
+    case EncryptSessionStorageStage::kClearOnly:
+      backend_task_runner_->PostTaskAndReplyWithResult(
+          FROM_HERE,
+          base::BindOnce(
+              &CommandStorageBackend::FlushAndReadCurrentSessionCommands,
+              backend_.get()),
+          base::BindOnce(&OnBackendReadFinished, /*saved_result=*/nullptr,
+                         std::move(callback)));
+      return;
+    case EncryptSessionStorageStage::kWriteBothReadOnlyClear:
+    case EncryptSessionStorageStage::kWriteBothReadPreferEncrypted:
+      backend_task_runner_->PostTaskAndReplyWithResult(
+          FROM_HERE,
+          base::BindOnce(
+              &FlushAndReadDualBackends, backend_, encrypted_backend_,
+              encrypt_stage ==
+                  EncryptSessionStorageStage::kWriteBothReadPreferEncrypted),
+          base::BindOnce(&OnBackendReadFinished, /*saved_result=*/nullptr,
+                         std::move(callback)));
+      return;
+    case EncryptSessionStorageStage::kWriteEncryptedReadPreferEncrypted:
+      backend_task_runner_->PostTaskAndReplyWithResult(
+          FROM_HERE,
+          base::BindOnce(
+              &CommandStorageBackend::FlushAndReadCurrentSessionCommands,
+              encrypted_backend_.get()),
+          base::BindOnce(&OnBackendReadFinished, /*saved_result=*/nullptr,
+                         std::move(callback)));
+      return;
+  }
+}
+
 bool CommandStorageManager::HasPendingSave() const {
   return weak_factory_for_timer_.HasWeakPtrs();
 }
diff --git a/components/sessions/core/command_storage_manager.h b/components/sessions/core/command_storage_manager.h
index cf6dec3b2ce69f19ebbb96155fa9707e6380664a..e22aefa816283c59d655cfbc07808239597fd9a3 100644
--- a/components/sessions/core/command_storage_manager.h
+++ b/components/sessions/core/command_storage_manager.h
@@ -110,6 +110,11 @@ class SESSIONS_EXPORT CommandStorageManager {
   // Passes all pending commands to the backend for saving.
   void Save();
 
+  // Saves pending commands immediately and returns a decoded receipt from the
+  // current session file. Dual-write stages succeed only when both backends
+  // return semantically identical command sequences.
+  void SaveAndReadCurrentSessionCommands(GetCommandsCallback callback);
+
   // Returns true if StartSaveTimer() has been called, but a save has not yet
   // occurred.
   bool HasPendingSave() const;
@@ -138,6 +143,11 @@ class SESSIONS_EXPORT CommandStorageManager {
  private:
   friend class CommandStorageManagerTestHelper;
 
+  // Implements Save(). `allow_empty_reset` permits an explicit marker-only
+  // truncation so verification can prove that an empty current session was
+  // fully materialized rather than reading a stale file.
+  void SaveImpl(bool allow_empty_reset);
+
   // Called by the backend if writing to the file failed.
   void OnErrorWritingToFile();
 
diff --git a/components/sessions/core/command_storage_manager_unittest.cc b/components/sessions/core/command_storage_manager_unittest.cc
index 775231132d0e93f2bee38fc4353400613306bc91..39df51442425931458559fd4791acf5c631c44f0 100644
--- a/components/sessions/core/command_storage_manager_unittest.cc
+++ b/components/sessions/core/command_storage_manager_unittest.cc
@@ -10,6 +10,7 @@
 #include "base/files/file_enumerator.h"
 #include "base/files/file_util.h"
 #include "base/files/scoped_temp_dir.h"
+#include "base/functional/callback_helpers.h"
 #include "base/strings/string_util.h"
 #include "base/test/bind.h"
 #include "base/test/metrics/histogram_tester.h"
@@ -17,6 +18,7 @@
 #include "base/test/scoped_feature_list.h"
 #include "base/test/task_environment.h"
 #include "components/os_crypt/async/browser/test_utils.h"
+#include "components/sessions/core/command_storage_backend.h"
 #include "components/sessions/core/command_storage_features.h"
 #include "components/sessions/core/command_storage_manager_delegate.h"
 #include "components/sessions/core/command_storage_manager_test_helper.h"
@@ -193,6 +195,138 @@ TEST_P(CommandStorageManagerTest, GetLastSessionCommands) {
   EXPECT_EQ(102U, commands[1]->id());
 }
 
+TEST_P(CommandStorageManagerTest, SaveAndReadCurrentSessionCommands) {
+  TestCommandStorageManagerDelegate delegate;
+  CommandStorageManager manager(GetParam().session_type, path_, &delegate,
+                                os_crypt_async_.get(), backend_task_runner_);
+  CommandStorageManagerTestHelper test_helper(&manager);
+  // Let an enabled encrypted backend initialize before requiring a receipt.
+  test_helper.RunMessageLoopUntilBackendDone();
+  manager.AppendRebuildCommand(std::make_unique<SessionCommand>(101, 0));
+  manager.AppendRebuildCommand(std::make_unique<SessionCommand>(102, 0));
+
+  std::vector<std::unique_ptr<SessionCommand>> commands;
+  bool error = true;
+  manager.SaveAndReadCurrentSessionCommands(base::BindLambdaForTesting(
+      [&commands, &error](
+          std::vector<std::unique_ptr<SessionCommand>> commands_out,
+          bool error_out) {
+        commands = std::move(commands_out);
+        error = error_out;
+      }));
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  EXPECT_FALSE(error);
+  ASSERT_EQ(2U, commands.size());
+  EXPECT_EQ(101U, commands[0]->id());
+  EXPECT_EQ(102U, commands[1]->id());
+  EXPECT_FALSE(manager.pending_reset());
+  EXPECT_TRUE(manager.pending_commands().empty());
+}
+
+TEST_P(CommandStorageManagerTest,
+       SaveAndReadCurrentSessionCommandsFailsClosedBeforeEncryptorIsReady) {
+  TestCommandStorageManagerDelegate delegate;
+  std::unique_ptr<os_crypt_async::OSCryptAsync> os_crypt =
+      os_crypt_async::GetTestOSCryptAsyncForTesting(
+          /*is_sync_for_unittests=*/false);
+  CommandStorageManager manager(GetParam().session_type, path_, &delegate,
+                                os_crypt.get(), backend_task_runner_);
+  CommandStorageManagerTestHelper test_helper(&manager);
+  manager.AppendRebuildCommand(std::make_unique<SessionCommand>(101, 0));
+
+  std::vector<std::unique_ptr<SessionCommand>> commands;
+  bool error = true;
+  manager.SaveAndReadCurrentSessionCommands(base::BindLambdaForTesting(
+      [&commands, &error](
+          std::vector<std::unique_ptr<SessionCommand>> commands_out,
+          bool error_out) {
+        commands = std::move(commands_out);
+        error = error_out;
+      }));
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  if (GetEncryptSessionStorageStage() ==
+      EncryptSessionStorageStage::kClearOnly) {
+    EXPECT_FALSE(error);
+    ASSERT_EQ(1U, commands.size());
+    EXPECT_EQ(101U, commands[0]->id());
+  } else {
+    EXPECT_TRUE(error);
+    EXPECT_TRUE(commands.empty());
+    EXPECT_TRUE(manager.pending_reset());
+    ASSERT_EQ(1U, manager.pending_commands().size());
+    EXPECT_EQ(101U, manager.pending_commands()[0]->id());
+  }
+}
+
+TEST_P(CommandStorageManagerTest,
+       SaveAndReadCurrentSessionCommandsRejectsDualWriteMismatch) {
+  TestCommandStorageManagerDelegate delegate;
+  CommandStorageManager manager(GetParam().session_type, path_, &delegate,
+                                os_crypt_async_.get(), backend_task_runner_);
+  CommandStorageManagerTestHelper test_helper(&manager);
+  test_helper.RunMessageLoopUntilBackendDone();
+  if (!test_helper.ShouldWriteCleartextFiles() ||
+      !test_helper.ShouldWriteEncryptedFiles()) {
+    GTEST_SKIP() << "The mismatch receipt applies only to dual-write stages.";
+  }
+
+  manager.AppendRebuildCommand(std::make_unique<SessionCommand>(101, 0));
+  manager.Save();
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  CommandStorageBackend* encrypted_backend = test_helper.GetEncryptedBackend();
+  ASSERT_TRUE(encrypted_backend);
+  test_helper.RunTaskOnBackendThread(
+      FROM_HERE, base::BindLambdaForTesting([encrypted_backend]() {
+        std::vector<std::unique_ptr<SessionCommand>> commands;
+        commands.push_back(std::make_unique<SessionCommand>(102, 0));
+        encrypted_backend->AppendCommands(std::move(commands),
+                                          /*truncate=*/false,
+                                          base::DoNothing());
+      }));
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  std::vector<std::unique_ptr<SessionCommand>> commands;
+  bool error = false;
+  manager.SaveAndReadCurrentSessionCommands(base::BindLambdaForTesting(
+      [&commands, &error](
+          std::vector<std::unique_ptr<SessionCommand>> commands_out,
+          bool error_out) {
+        commands = std::move(commands_out);
+        error = error_out;
+      }));
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  EXPECT_TRUE(error);
+  EXPECT_TRUE(commands.empty());
+}
+
+TEST_P(CommandStorageManagerTest,
+       SaveAndReadCurrentSessionCommandsWritesEmptyReset) {
+  TestCommandStorageManagerDelegate delegate;
+  CommandStorageManager manager(GetParam().session_type, path_, &delegate,
+                                os_crypt_async_.get(), backend_task_runner_);
+  CommandStorageManagerTestHelper test_helper(&manager);
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  std::vector<std::unique_ptr<SessionCommand>> commands;
+  bool error = true;
+  manager.SaveAndReadCurrentSessionCommands(base::BindLambdaForTesting(
+      [&commands, &error](
+          std::vector<std::unique_ptr<SessionCommand>> commands_out,
+          bool error_out) {
+        commands = std::move(commands_out);
+        error = error_out;
+      }));
+  test_helper.RunMessageLoopUntilBackendDone();
+
+  EXPECT_FALSE(error);
+  EXPECT_TRUE(commands.empty());
+  EXPECT_FALSE(manager.pending_reset());
+}
+
 TEST_P(CommandStorageManagerTest, OnErrorWritingSessionCommands) {
   TestCommandStorageManagerDelegate delegate;
   CommandStorageManager manager(GetParam().session_type, path_, &delegate,
