diff --git a/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.cc b/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.cc
index e5409f1de216d9a155a68be8d536473e6fce91b0..d243a6752bd8ad2f4085f74c9ab5eb06d80f8a2d 100644
--- a/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.cc
+++ b/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.cc
@@ -7,6 +7,7 @@
 #include <cstdint>
 #include <memory>
 #include <optional>
+#include <utility>
 
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/to_vector.h"
@@ -306,6 +307,11 @@ BookmarkMenuDelegate::~BookmarkMenuDelegate() {
   bookmark_merged_service_observation_.Reset();
 }
 
+void BookmarkMenuDelegate::SetContextMenuPresentationCallback(
+    ContextMenuPresentationCallback callback) {
+  context_menu_presentation_callback_ = std::move(callback);
+}
+
 void BookmarkMenuDelegate::BuildFullMenu(MenuItemView* parent) {
   CHECK(!parent_menu_item_);
   CHECK(parent);
@@ -659,6 +665,12 @@ void BookmarkMenuDelegate::RunContextMenuAt(
       parent_, browser_, profile_, location_, nodes, close_on_remove,
       can_paste);
   bookmark_context_menu_observation_.Observe(context_menu_.get());
+  if (context_menu_presentation_callback_ &&
+      !context_menu_presentation_callback_.Run(context_menu_->menu())) {
+    bookmark_context_menu_observation_.Reset();
+    context_menu_.reset();
+    return;
+  }
   context_menu_->RunMenuAt(p, source_type);
 }
 
diff --git a/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.h b/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.h
index 0377020eb0845b3b27e298e7ee71a7e84316d71f..a63f0edaba2af76c3e5ad8892ab4818c4479bdbe 100644
--- a/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.h
+++ b/chrome/browser/ui/views/bookmarks/bookmark_menu_delegate.h
@@ -66,6 +66,14 @@ class BookmarkMenuDelegate : public BookmarkMergedSurfaceServiceObserver,
 
   ~BookmarkMenuDelegate() override;
 
+  // Runs after the clipboard check and menu creation, immediately before
+  // showing it. Embedders may customize presentation or return false when the
+  // originating surface is no longer valid. Native model/permission ownership
+  // and behavior for callers without a callback are unchanged.
+  using ContextMenuPresentationCallback =
+      base::RepeatingCallback<bool(views::MenuItemView*)>;
+  void SetContextMenuPresentationCallback(ContextMenuPresentationCallback callback);
+
   // Extends the `parent` menu by adding items for all relevant bookmark nodes,
   // including:
   // - a folder for managed nodes, if any
@@ -371,6 +379,8 @@ class BookmarkMenuDelegate : public BookmarkMergedSurfaceServiceObserver,
   // enable mnemonics.
   bool menu_uses_mnemonics_ = false;
 
+  ContextMenuPresentationCallback context_menu_presentation_callback_;
+
   base::ScopedObservation<BookmarkContextMenu, BookmarkContextMenuObserver>
       bookmark_context_menu_observation_{this};
 
