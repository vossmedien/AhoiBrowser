diff --git a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
index bafc0c4a9bf823f52800d608ad386ffd37be0ffc..8459249c31a9f9d8c2ebf158ef2c1d1fda0369eb 100644
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -1081,9 +1081,20 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
   views().multi_contents_view->SetShouldShowTopSeparator(
       !IsAhoiShell(views()) && separator_info.multi_contents_separator);
 
+  const bool is_ahoi_shell = IsAhoiShell(views());
+  const bool uses_ahoi_content_card =
+      is_ahoi_shell && !is_fullscreen(layout_data_->window_state);
+  views().multi_contents_view->SetResizeHandlesAlwaysVisible(
+      horizontal_layout.is_split_view && uses_ahoi_content_card);
+
   // Updating the top, left and right insets for contents container view when
   // in split view. This is dependent on a number of other browser states.
-  if (horizontal_layout.is_split_view) {
+  if (horizontal_layout.is_split_view && uses_ahoi_content_card) {
+    // The Ahoi card already supplies the complete outer gutter. Retaining
+    // Chromium's split inset here exposes the background as a second 8 px
+    // seam between the sidebar and the first pane.
+    views().multi_contents_view->SetSplitViewInsets(gfx::Insets());
+  } else if (horizontal_layout.is_split_view) {
     const bool include_top_inset =
         layout_data_->window_state == WindowState::kFullscreen ||
         infobar_visible;
@@ -1141,8 +1152,7 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
   // Ahoi's content card is true layout rather than an overlay. This keeps
   // CSS media queries, window.innerWidth/innerHeight, DevTools and split-pane
   // hit testing in sync with the pixels the user actually sees.
-  const bool is_ahoi_shell = IsAhoiShell(views());
-  if (is_ahoi_shell && !is_fullscreen(layout_data_->window_state)) {
+  if (uses_ahoi_content_card) {
     contents_layout.bounds.Inset(
         gfx::Insets(ahoi::visual_style::kContentCardInset));
   }
@@ -1188,7 +1198,7 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
     }
   }
 
-  if (is_ahoi_shell && !is_fullscreen(layout_data_->window_state)) {
+  if (uses_ahoi_content_card) {
     views().multi_contents_view->SetBackgroundRadii(
         gfx::RoundedCornersF(ahoi::visual_style::kContentCardCornerRadius));
   } else if (in_glass_mode()) {
diff --git a/chrome/browser/ui/views/frame/multi_contents_resize_area.cc b/chrome/browser/ui/views/frame/multi_contents_resize_area.cc
index 2458693c4938495e974f3e2786cad0c39528686e..4a708567ef98f75d59407d4850af20193b1d574f 100644
--- a/chrome/browser/ui/views/frame/multi_contents_resize_area.cc
+++ b/chrome/browser/ui/views/frame/multi_contents_resize_area.cc
@@ -55,9 +55,18 @@ MultiContentsResizeHandle::MultiContentsResizeHandle() {
       kColorSidePanelHoverResizeAreaHandle, kHandleCornerRadius));
 }
 
+void MultiContentsResizeHandle::SetAlwaysVisible(bool always_visible) {
+  if (always_visible_ == always_visible) {
+    return;
+  }
+  always_visible_ = always_visible;
+  UpdateVisibility();
+}
+
 void MultiContentsResizeHandle::UpdateVisibility() {
   layer()->SetVisible(parent()->GetVisible() &&
-                      (HasFocus() || parent()->IsMouseHovered()));
+                      (always_visible_ || HasFocus() ||
+                       parent()->IsMouseHovered()));
 }
 
 void MultiContentsResizeHandle::AddedToWidget() {
@@ -116,6 +125,10 @@ void MultiContentsResizeArea::SetLayout(
   }
 }
 
+void MultiContentsResizeArea::SetHandleAlwaysVisible(bool always_visible) {
+  resize_handle_->SetAlwaysVisible(always_visible);
+}
+
 void MultiContentsResizeArea::OnResize(int resize_amount, bool done_resizing) {
   multi_contents_view_->OnResize(divider_index_, resize_amount, done_resizing);
 }
diff --git a/chrome/browser/ui/views/frame/multi_contents_resize_area.h b/chrome/browser/ui/views/frame/multi_contents_resize_area.h
index 5356e211bca6c7c079ab044b0ea5295a6aa1b8a1..108dfb62c1720b697ed49c291cae49be049dec60 100644
--- a/chrome/browser/ui/views/frame/multi_contents_resize_area.h
+++ b/chrome/browser/ui/views/frame/multi_contents_resize_area.h
@@ -37,6 +37,7 @@ class MultiContentsResizeHandle : public views::View,
 
   MultiContentsResizeHandle();
 
+  void SetAlwaysVisible(bool always_visible);
   void UpdateVisibility();
 
   // views::View:
@@ -45,6 +46,9 @@ class MultiContentsResizeHandle : public views::View,
 
   // FocusChangeListener:
   void OnDidChangeFocus(views::View* before, views::View* now) override;
+
+ private:
+  bool always_visible_ = false;
 };
 
 // ResizeArea meant to draw in between WebContents within a MultiContentsView,
@@ -64,6 +68,7 @@ class MultiContentsResizeArea : public views::ResizeArea,
 
   void SetLayout(const split_tabs::SplitTabVisualData& visual_data,
                  size_t pane_count);
+  void SetHandleAlwaysVisible(bool always_visible);
 
   MultiContentsResizeHandle* resize_handle_for_testing() const {
     return resize_handle_;
diff --git a/chrome/browser/ui/views/frame/multi_contents_view.cc b/chrome/browser/ui/views/frame/multi_contents_view.cc
index d3842b791337d5710090b5a1a38f002df089e001..b0aaf496c47f9036c7a674fabe887ed222136e0d 100644
--- a/chrome/browser/ui/views/frame/multi_contents_view.cc
+++ b/chrome/browser/ui/views/frame/multi_contents_view.cc
@@ -1281,6 +1281,12 @@ void MultiContentsView::SetShouldShowTopSeparator(bool should_show) {
   InvalidateLayout(/*avoid_propagate_during_layout=*/true);
 }
 
+void MultiContentsView::SetResizeHandlesAlwaysVisible(bool always_visible) {
+  for (auto& resize_area : resize_areas_) {
+    resize_area->SetHandleAlwaysVisible(always_visible);
+  }
+}
+
 void MultiContentsView::SetSplitViewInsets(const gfx::Insets& insets) {
   if (split_view_insets_ == insets) {
     return;
diff --git a/chrome/browser/ui/views/frame/multi_contents_view.h b/chrome/browser/ui/views/frame/multi_contents_view.h
index 1dfc4afac1899f9b4f02d664adc168296c0ef600..86041ceecf08e4860484f94699d38bcf022f70a7 100644
--- a/chrome/browser/ui/views/frame/multi_contents_view.h
+++ b/chrome/browser/ui/views/frame/multi_contents_view.h
@@ -207,6 +207,7 @@ class MultiContentsView
   void OnDragAndDropPrefStateChange();
 
   void SetShouldShowTopSeparator(bool should_show);
+  void SetResizeHandlesAlwaysVisible(bool always_visible);
   void SetSplitViewInsets(const gfx::Insets& insets);
   const gfx::Insets& split_view_insets_for_testing() const {
     return split_view_insets_;
